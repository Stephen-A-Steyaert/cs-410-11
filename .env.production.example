# GitHub repository (e.g., username/repo-name or org/repo-name)
GITHUB_REPOSITORY=stephen-steyaert-odu/cs-410-11

# Note: Sensitive secrets (SECRET_KEY, Cloudflare credentials) are stored
# as Docker Swarm secrets, not environment variables.
# Run: ./setup-secrets.sh to configure them securely.
